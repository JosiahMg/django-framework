# Django Framework

#### 功能
Django 的基本框架


#### 运行
python manage.py runserver

#### 日志
common/log_utils.py 修改日志名

#### url
api/url.py

#### docker
docker-compose up -d

#### 配置
conf/config.py  & config_dev.yaml & config_test.yaml
